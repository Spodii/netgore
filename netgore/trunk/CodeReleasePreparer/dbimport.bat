echo This file will import the database dump from its default location
echo You will probably need to change the database connection strings

mysql --user=root --password= --host=localhost demogame < ..\db.sql