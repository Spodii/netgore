using System.Linq;
using DemoGame;
using NetGore;
using NetGore.RPGComponents;

namespace NetGore.Db.ClassCreator
{
    public enum MemberVisibilityLevel
    {
        Private,
        Protected,
        Internal,
        ProtectedInternal,
        Public
    }
}