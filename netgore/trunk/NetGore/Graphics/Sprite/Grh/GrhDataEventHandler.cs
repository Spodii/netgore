using System.Linq;

namespace NetGore.Graphics
{
    /// <summary>
    /// Handles events from the <see cref="GrhData"/>.
    /// </summary>
    /// <param name="sender">The <see cref="GrhData"/> that the event came from.</param>
    public delegate void GrhDataEventHandler(GrhData sender);
}