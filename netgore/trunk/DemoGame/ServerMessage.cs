using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DemoGame
{
    /// <summary>
    /// Enum containing the ID of each text message sent from the Server to the Client. These are
    /// the IDs used to represent messages sent with the ServerPacketID.SendMessage enum.
    /// </summary>
    public enum ServerMessage : byte
    {
        // NOTE: Add server messages here
    }
}
